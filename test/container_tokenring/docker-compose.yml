version: '3.8'

services:
  gateway1:
    build: .
    container_name: gateway1
    privileged: true
    networks:
      - test-network

  gateway2:
    build: .
    container_name: gateway2
    privileged: true
    networks:
      - test-network

networks:
  test-network: